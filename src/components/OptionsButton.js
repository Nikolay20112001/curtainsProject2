import React from 'react';
import '../css/OptionsButton.css';

class OptionsButton extends React.Component {
    render() {
      return (
        <button className='OptionsButton'>Выбрать опции</button>
      );
    }
  }
  
  
  export default OptionsButton;